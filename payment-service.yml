server:
  port: 9199

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airline_payment_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root@root
    driver-class-name: com.mysql.cj.jdbc.Driver


  kafka:
    bootstrap-servers: localhost:9092

razorpay:
  api:
    key: rzp_test_TbQ4t7ht0aS9pz
    secret: 95U8sfKlCRlM49NvLABCqblG

  callback:
    base-url: http://localhost:9199/

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9200/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: false