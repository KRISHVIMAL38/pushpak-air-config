server:
  port: 9194

spring:


  datasource:
    url: jdbc:mysql://localhost:3306/airline_flight_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root@root
    driver-class-name: com.mysql.cj.jdbc.Driver


  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: flight-ops-service-group

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9200/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: false